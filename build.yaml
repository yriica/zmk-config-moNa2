# Temporary right-half build for PMW3610 startup diagnostics.
include:
  - board: seeeduino_xiao_ble
    shield: moNa2_R rgbled_adapter
    snippet: zmk-usb-logging
    cmake-args: -DCONFIG_INPUT_LOG_LEVEL_INF=y -DCONFIG_ZMK_LOG_LEVEL_WRN=y -DCONFIG_LOG_PROCESS_THREAD_STARTUP_DELAY_MS=15000 -DCONFIG_LOG_BUFFER_SIZE=8192
    artifact-name: moNa2_R-trackball-diagnostic
